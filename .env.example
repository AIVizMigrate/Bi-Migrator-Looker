# Looker DAX API configuration
# AI conversion is enabled by default; falls back to rule-based if API is unavailable.
# Host-only values are supported and normalized to:
#   https://<host>/looker/convert
DAX_API_URL=https://daxapidemo.azurewebsites.net
